name: Build docker test stage - feature
on:
  push:
    branches:
      - feature
jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
    - name: Build and test Docker image
      uses: actions/docker/cli@v2
      with:
        args: build -t my-image .
        push: false
